import React from "react";
import "./VerificationLayout.css"

const VerifcationLayout = ({ children }) => {
  return (
    <div className="verification-container">
      {children}
    </div>
  );
};

export default VerifcationLayout;
